<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> | Matuka Járműklíma Blog</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/blog.css">
    <style>
 
    </style>
</head>
<body style="min-height:100vh;display:flex;flex-direction:column;">
    <%- include('_nav') %>
    <section class="post-section" style="flex:1 0 auto;">
        <h2 class="post-title"><%= post.title %></h2>
        <div class="post-meta">
            Szerző: <%= post.author %> &middot; Létrehozva: <%= new Date(post.createdAt).toLocaleDateString('hu-HU') %>
        </div>
        <hr>
        <div class="post-content">
            <%= post.content %>
        </div>
        <% if (post.tags && post.tags.length > 0) { %>
            <div class="post-tags">
                <% post.tags.forEach(tag => { %>
                    <span><%= tag %></span>
                <% }) %>
            </div>
        <% } %>
        <% if (typeof session !== 'undefined' && session && session.userId) { %>
        <div class="post-actions">
            <a href="/blog/dashboard" class="btn btn-edit">Vissza a vezérlőpultra</a>
            <a href="/blog/posts/<%= post.id || post._id %>/edit" class="btn btn-edit">Szerkesztés</a>
        </div>
        <% } %>
    </section>
    <footer style="flex-shrink:0;">
        <p>&copy; <%= new Date().getFullYear() %> Matuka Járműklíma Blog. Minden jog fenntartva.</p>
    </footer>
</body>
</html>
